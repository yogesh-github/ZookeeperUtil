ZookeeperUtil is a collection of utility tools for Zookeeper.

PrintTree util prints all zookeeper files / nodes in zookeeper file systems in linux tree command format 
and in XML format.

input arguments:
path zookeeperConnectionString treeType